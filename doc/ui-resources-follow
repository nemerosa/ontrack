client:Client
interceptor:Interceptor
uip:UIProjects
uid:UIBranches
assembler:ResourceAssembler
/resource:Resource
rp:ProjectRepository
rb:BranchRepository

client:json=interceptor.getProject(p, follow:branches)
interceptor:resource=uip.getProject(p)
uip:project=rp.getById(p)
uip:resource=assembler.toProjectResource(project)
assembler:resource.new
assembler:resource.link('branches', uri + callback to UIBranch)
interceptor:resource.Get 'branches' link callback
interceptor:branchesResource=uid.getBranchesForProject(p)
uid:branches=rb.getBranchesForProject(p)
interceptor:resource.Replace link with resource
