import {Typography} from "antd";

export default function ManualAdmissionRuleCheck({check, ruleConfig, ruleData}) {
    return (
        <>
            {/* TODO Displays details about the approval */}
            <Typography.Text>Deployment must be approved manually.</Typography.Text>
        </>
    )
}