[[event-new_promotion_level]]
=== new_promotion_level

When a promotion level is created.

Default template:

[source]
----
New promotion level ${promotionLevel} for branch ${branch} in ${project}.
----

