[[event-new_validation_stamp]]
=== new_validation_stamp

When a validation stamp is created.

Default template:

[source]
----
New validation stamp ${validationStamp} for branch ${branch} in ${project}.
----

