[[event-image_promotion_level]]
=== image_promotion_level

When a promotion level's image is updated.

Default template:

[source]
----
Image for promotion level ${promotionLevel} for branch ${branch} in ${project} has changed.
----

