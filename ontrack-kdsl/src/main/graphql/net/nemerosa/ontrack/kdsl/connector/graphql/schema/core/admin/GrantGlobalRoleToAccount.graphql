mutation GrantGlobalRoleToAccount(
    $accountId: Int!,
    $globalRole: String!,
) {
    grantGlobalRoleToAccount(input: {
        accountId: $accountId,
        globalRole: $globalRole,
    }) {
        ...PayloadUserErrors
    }
}