mutation LaunchWorkflow(
    $workflowId: String!,
    $context: JSON!,
) {
    launchWorkflow(input: {
        workflowId: $workflowId,
        context: $context,
    }) {
        ...PayloadUserErrors
        workflowInstanceId
    }
}