query PipelineRequiredInputs(
    $id: String!,
) {
    slotPipelineById(id: $id) {
        requiredInputs {
            config {
                id
                ruleId
            }
        }
    }
}
