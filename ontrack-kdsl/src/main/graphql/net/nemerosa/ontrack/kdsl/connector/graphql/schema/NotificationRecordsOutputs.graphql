query NotificationRecordsOutputs(
    $channel: String,
) {
    notificationRecords(channel: $channel) {
        pageItems {
            result {
                type
                message
                output
            }
        }
    }
}