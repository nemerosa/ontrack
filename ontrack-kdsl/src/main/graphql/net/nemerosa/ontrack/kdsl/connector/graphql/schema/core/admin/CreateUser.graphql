mutation CreateUser(
    $fullName: String!,
    $email: String!,
) {
    createTestAccount(input: {
        fullName: $fullName,
        email: $email,
    }) {
        account {
            id
        }
        ...PayloadUserErrors
    }
}
