mutation CreateUser(
    $fullName: String!,
    $email: String!,
) {
    createBuiltInAccount(input: {
        fullName: $fullName,
        email: $email,
    }) {
        account {
            id
        }
        ...PayloadUserErrors
    }
}
