mutation CreateConfiguration(
    $name: String!,
    $type: String!,
    $data: JSON!,
) {
    createConfiguration(input: {
        name: $name,
        type: $type,
        data: $data,
    }) {
        ...PayloadUserErrors
    }
}